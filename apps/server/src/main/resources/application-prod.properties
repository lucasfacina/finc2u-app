# Perfil de produção — PostgreSQL. Credenciais SEMPRE via variáveis de ambiente.
# Descomente e configure ao migrar de H2 para PostgreSQL (ver CLAUDE.md raiz).
#spring.datasource.url=jdbc:postgresql://localhost:5432/finc2u
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=false
#spring.h2.console.enabled=false
#app.seeders.enabled=false